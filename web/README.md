# Pacioli Web UI

## โครงสร้าง

```
web/
├── api/                    # Hono API Backend (Bun)
│   ├── index.ts           # Main server
│   ├── package.json
│   └── routes/
│       ├── customers.ts   # CRUD ลูกค้า
│       ├── documents.ts   # CRUD เอกสาร
│       ├── config.ts      # จัดการ freelancer config
│       ├── generate.ts    # สร้าง PDF
│       ├── chat.ts        # Smart Parser
│       └── ai.ts          # OpenRouter AI API
│
└── app/                    # React Frontend (Vite)
    ├── package.json
    ├── vite.config.ts
    └── src/
        ├── components/
        │   ├── ui/        # shadcn/ui components
        │   ├── forms/     # Multi-step form components
        │   ├── chat/      # AI Chat widget
        │   └── ai/        # AI Modals (Settings, Generator)
        ├── hooks/         # Custom hooks
        ├── pages/         # Page components
        ├── types/         # TypeScript types
        └── lib/           # Utilities
```

## การติดตั้ง

```bash
# ติดตั้ง dependencies ทั้ง API และ App
bun run web:install
```

## การใช้งาน

### Development Mode

```bash
# รัน API server (port 3001)
bun run web:api

# รัน React app (port 3000) - ใน terminal อื่น
bun run web:app

# หรือรันทั้งคู่พร้อมกัน
bun run web:dev
```

เปิดเบราว์เซอร์ไปที่ http://localhost:3000

## Features

### Multi-step Form
1. **ผู้ออกเอกสาร** - กรอกข้อมูล freelancer ของคุณ
2. **ลูกค้า** - เลือกหรือสร้างลูกค้าใหม่
3. **รายละเอียด** - เลือกประเภทเอกสาร เพิ่มรายการ ตั้งค่าภาษี
4. **ยืนยัน** - ตรวจสอบและสร้าง PDF

### AI Features (OpenRouter)

#### 1. สร้างรายการด้วย AI
ในขั้นตอน "รายละเอียดเอกสาร":
- คลิกปุ่ม **"สร้างด้วย AI"**
- กรอกรายละเอียดงาน เช่น "พัฒนาเว็บไซต์ e-commerce ด้วย React"
- เลือกจำนวนรายการที่ต้องการ (1-10)
- AI จะสร้างรายการสินค้า/บริการให้
- คุณใส่จำนวนและราคาเองภายหลัง

#### 2. AI Chat
คลิกปุ่ม 💬 มุมขวาล่าง:
- **Smart Parser** - detect ข้อมูลลูกค้าจากข้อความธรรมชาติ
- **AI Chat** - สนทนากับ AI ได้อย่างอิสระ (ใช้ OpenRouter)

#### 3. AI Settings
คลิกไอคอน ⚙️ ใน Chat window:
- ตั้งค่า OpenRouter API Key
- เลือก AI Model ที่ต้องการใช้

### AI Models ที่รองรับ
| Model | Provider | หมายเหตุ |
|-------|----------|---------|
| Gemini 2.5 Flash Preview | Google | เร็ว ราคาถูก |
| Gemini 2.5 Pro Preview | Google | ฉลาดกว่า |
| GPT-4.1 Mini | OpenAI | สมดุล |
| Claude Sonnet 4 | Anthropic | ละเอียด |
| DeepSeek V3 | DeepSeek | ราคาถูก |

### PDF Preview
หลังสร้างเอกสารสำเร็จ:
- คลิก "ดู Preview" เพื่อดู PDF ใน browser
- คลิก "เปิด PDF" เพื่อเปิดในหน้าต่างใหม่

## API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | /api/customers | รายชื่อลูกค้าทั้งหมด |
| GET | /api/customers/:id | ดึงข้อมูลลูกค้า |
| POST | /api/customers | สร้างลูกค้าใหม่ |
| PUT | /api/customers/:id | แก้ไขลูกค้า |
| DELETE | /api/customers/:id | ลบลูกค้า |
| GET | /api/documents | รายการเอกสารทั้งหมด |
| POST | /api/generate | สร้าง PDF |
| POST | /api/chat | Smart Parser Chat |
| GET | /api/config/freelancer | ดึง freelancer config |
| PUT | /api/config/freelancer | บันทึก freelancer config |
| GET | /api/ai/settings | ดึง AI settings |
| PUT | /api/ai/settings | บันทึก AI settings |
| POST | /api/ai/generate-items | สร้างรายการด้วย AI |
| POST | /api/ai/chat | AI Chat (OpenRouter) |
| GET | /api/output | รายการ PDF ที่สร้างแล้ว |
| GET | /output/:filename | ดาวน์โหลด/ดู PDF |
